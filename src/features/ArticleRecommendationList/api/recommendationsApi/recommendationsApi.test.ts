import { useArticleRecommendationList } from './recommendationsApi';

describe('f', () => {
    it('f', () => {
        const i = useArticleRecommendationList;
        expect(true).toBe(true);
    });
});
